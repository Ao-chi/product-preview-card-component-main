import "../assets/sass/components/image.scss";

const Image = () => {
    return (
        <div className="card-image">
            {/* <img className="img-fluid" src={props.cardImage.image} alt={props.cardImage.alt} /> */}
            <div className="image"></div>
        </div>
    );
};

export default Image;
